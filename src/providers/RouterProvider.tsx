import React from "react";

const RouterProvider: React.FC<{ children: React.ReactNode }> = ({ children }) => {
	// Placeholder: can provide additional router-level wrappers if needed
	return <>{children}</>;
};

export default RouterProvider;
