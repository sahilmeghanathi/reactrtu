import React from "react";

const ThemeProvider: React.FC<{ children: React.ReactNode }> = ({ children }) => {
	// Placeholder: provide theme context or CSS variables
	return <>{children}</>;
};

export default ThemeProvider;
