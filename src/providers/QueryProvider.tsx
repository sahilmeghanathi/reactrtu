import React from "react";

const QueryProvider: React.FC<{ children: React.ReactNode }> = ({ children }) => {
	// Placeholder: wrap with react-query's QueryClientProvider if used
	return <>{children}</>;
};

export default QueryProvider;
